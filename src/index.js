import { SafeEventEmitter } from './eventEmitter.js';
import { Observable } from './observable.js';

console.log('=== Lab 7: Reactive Communication ===');
console.log(`running: ${SafeEventEmitter.name}, ${Observable.name}`);

import('./demo.js');